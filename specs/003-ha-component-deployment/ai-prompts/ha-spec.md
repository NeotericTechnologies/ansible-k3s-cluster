The goal of this pass is to ensure all core and addon components are deployed in a high availability configuration when the cluster is provisioned in a high availability configuration (3 or more servers, zero or more agents).

For example, the csi-nfs-controller recommends at least 2 controller replicas for HA deployments.
