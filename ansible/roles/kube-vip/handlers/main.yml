---
# ansible/roles/kube-vip/handlers/main.yml
# Purpose: Handlers for kube-vip role

- name: Restart k3s
  ansible.builtin.systemd:
    name: k3s
    state: restarted
  when: inventory_hostname in groups['k3s_servers']
